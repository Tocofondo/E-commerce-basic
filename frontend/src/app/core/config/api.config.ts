// URL base de la API del backend. En desarrollo apunta al uvicorn local
// (ver backend/README.md); reemplazar antes de un build de producción.
export const API_BASE_URL = 'http://localhost:8000/api/v1';
